# Golf Swing Capture - Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
PyQt6>=6.5.0

# Audio (optional but recommended for sound triggering)
PyAudio>=0.2.13

# Machine learning (optional - enables learned audio classifier)
scikit-learn>=1.3.0

# QR codes for phone setup (optional - falls back to text links)
qrcode>=7.4

# Note: On Windows, you may need to install PyAudio via:
# pip install pipwin
# pipwin install pyaudio
#
# Or download the wheel from:
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
